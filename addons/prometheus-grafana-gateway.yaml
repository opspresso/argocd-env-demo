apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: prometheus-grafana-gateway
spec:
  generators:
    - list:
        elements:
          - cluster: eks-demo
            values:
              host: "grafana.istio.bruce.spic.me"

  template:
    metadata:
      name: "prometheus-grafana-gateway-{{cluster}}"
      annotations:
        notifications.argoproj.io/subscribe.on-sync-succeeded.slack: noti-argocd-alpha-addon
      labels:
        app.kubernetes.io/instance: "addons-{{cluster}}"
        krmt.io/cluster: "{{cluster}}"
        krmt.io/group: "addons"
    spec:
      project: default
      destination:
        name: "{{cluster}}"
        namespace: addon-prometheus
      source:
        repoURL: https://charts.helm.sh/incubator
        targetRevision: "0.2.5"
        chart: raw
        helm:
          values: |-
            fullnameOverride: prometheus-grafana-gateway

            resources:
              - apiVersion: networking.istio.io/v1beta1
                kind: Gateway
                metadata:
                  name: prometheus-grafana
                spec:
                  selector:
                    istio: ingressgateway
                  servers:
                    - hosts:
                        - {{values.host}}
                      port:
                        number: 80
                        name: http
                        protocol: HTTP

              - apiVersion: networking.istio.io/v1beta1
                kind: VirtualService
                metadata:
                  name: prometheus-grafana
                spec:
                  hosts:
                    - {{values.host}}
                  gateways:
                    - prometheus-grafana
                  http:
                    - route:
                        - destination:
                            host: prometheus-eks-demo-grafana
                            port:
                              number: 80

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
