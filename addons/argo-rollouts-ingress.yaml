apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argo-rollouts-ingress
spec:
  generators:
    - list:
        elements:
          - cluster: eks-demo
            values:
              host: "argo-rollouts.bruce.spic.me"

  template:
    metadata:
      name: "argo-rollouts-ingress-{{cluster}}"
      annotations:
        notifications.argoproj.io/subscribe.on-sync-succeeded.slack: noti-argocd-alpha-addon
      labels:
        app.kubernetes.io/instance: "addons-{{cluster}}"
        krmt.io/cluster: "{{cluster}}"
        krmt.io/group: "addons"
    spec:
      project: default
      destination:
        name: "{{cluster}}"
        namespace: addon-argo-rollouts
      source:
        repoURL: https://charts.helm.sh/incubator
        targetRevision: "0.2.5"
        chart: raw
        helm:
          values: |-
            fullnameOverride: argo-rollouts-ingress

            resources:
              - apiVersion: networking.k8s.io/v1
                kind: Ingress
                metadata:
                  annotations:
                    kubernetes.io/ingress.class: nginx
                    kubernetes.io/ssl-redirect: 'true'
                  name: argo-rollouts
                spec:
                  rules:
                    - host: "{{values.host}}"
                      http:
                        paths:
                          - backend:
                              service:
                                name: argo-rollouts-dashboard
                                port:
                                  number: 3100
                            path: /

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
