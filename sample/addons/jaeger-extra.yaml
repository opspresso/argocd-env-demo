apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: jaeger-extra
spec:
  generators:
    - list:
        elements:
          - cluster: eks-demo
            values:
              host: "demo.nalbam.com"

  template:
    metadata:
      name: "jaeger-extra-{{cluster}}"
      annotations:
        notifications.argoproj.io/subscribe.on-sync-succeeded.slack: noti-argocd-{{cluster_env}}-addon
      labels:
        app.kubernetes.io/instance: "addons-{{cluster}}"
        krmt.io/cluster: "{{cluster}}"
        krmt.io/group: "addons"
    spec:
      project: addons
      destination:
        name: "{{cluster}}"
        namespace: addon-tracing
      source:
        repoURL: https://charts.helm.sh/incubator
        targetRevision: "0.2.5"
        chart: raw
        helm:
          values: |-
            fullnameOverride: jaeger-extra

            resources:
              - apiVersion: networking.istio.io/v1beta1
                kind: Gateway
                metadata:
                  name: jaeger
                spec:
                  selector:
                    istio: ingressgateway
                  servers:
                    - hosts:
                        - "jaeger.{{hostname}}"
                      port:
                        number: 80
                        name: http
                        protocol: HTTP

              - apiVersion: networking.istio.io/v1beta1
                kind: VirtualService
                metadata:
                  name: jaeger
                spec:
                  hosts:
                    - "jaeger.{{hostname}}"
                  gateways:
                    - jaeger
                  http:
                    - route:
                        - destination:
                            host: jaeger-query
                            port:
                              number: 80

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
