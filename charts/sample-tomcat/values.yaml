app:
  fullnameOverride: sample-tomcat

  image:
    repository: 082867736673.dkr.ecr.ap-northeast-2.amazonaws.com/nalbam/sample-tomcat

  resources:
    limits:
      cpu: "1"
      memory: 1Gi
    requests:
      cpu: "1"
      memory: 1Gi

  service:
    name: http
    port: 8080

  readinessProbe:
    failureThreshold: 3
    httpGet:
      path: /
      port: 8080
    initialDelaySeconds: 30
    periodSeconds: 10

  livenessProbe:
    failureThreshold: 3
    httpGet:
      path: /
      port: 8080
    initialDelaySeconds: 30
    periodSeconds: 10

  # strategy:
  #   blueGreen:
  #     activeService: sample-tomcat
  #     autoPromotionEnabled: true
  #     autoPromotionSeconds: 30
  #     previewService: sample-tomcat-preview
