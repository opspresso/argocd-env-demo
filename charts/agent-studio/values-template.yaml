app:
  configmap:
    enabled: true
    data:
      ADMIN_EMAILS: me@nalbam.com
      ALLOWED_EMAIL_DOMAINS: nalbam.com
      AWS_REGION: ap-northeast-2
      BETTER_AUTH_URL: https://agent-studio.{{hostname.public}}
      DYNAMODB_TABLE_NAME: agent-studio
      LLM_BASE_URL: https://api.openai.com/v1
      LLM_PROVIDER_ANTHROPIC_BASE_URL: https://api.anthropic.com/v1
      LLM_PROVIDER_OPENAI_BASE_URL: https://api.openai.com/v1
      LLM_PROVIDER_XAI_BASE_URL: https://api.x.ai
      MAX_RUN_DURATION_MS: '600000'
      # The MCP servers in the agent-mcps chart are cluster-internal, so their
      # Service names resolve to ClusterIPs the SSRF guard blocks. Declaring the
      # namespace suffix is what lets them be registered and called; the blocked
      # ranges themselves are untouched. See docs/SECURITY.md in agent-studio.
      MCP_INTERNAL_HOST_SUFFIXES: agent-mcps.svc.cluster.local
      PUBLIC_BASE_URL: https://agent-studio.{{hostname.public}}
      S3_BUCKET_NAME: agent-studio-static
      SKILLS_REPO_BRANCH: main
      SKILLS_REPO: opspresso/agent-skills
      STAGE: 'prod'
  routing:
    hosts:
    - agent-studio.{{hostname.public}}
    httpRoute:
      enabled: true
  podLabels:
    sidecar.istio.io/inject: "true"
  podAnnotations:
    link.argocd.argoproj.io/external-link: https://agent-studio.{{hostname.public}}

  serviceMonitor:
    labels:
      release: prometheus-{{cluster}}
