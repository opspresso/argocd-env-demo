apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-argocd
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mcp-argocd
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: agent-studio
          podSelector:
            matchLabels:
              app.kubernetes.io/name: agent-studio
      ports:
        - protocol: TCP
          port: 3000
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: mcp-argocd
      ports:
        - protocol: TCP
          port: 3000
