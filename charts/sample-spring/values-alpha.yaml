app:
  configmap:
    data:
      CLUSTER_NAME: eks-demo
      HOSTNAME: sample.svc.cluster.local
      MESSAGE: hello world
      PROFILE: alpha
      PROTOCOL: http
      VERSION: v0.8.63
    enabled: true
  env:
  - name: ENV_HASH
    value: b23dc40efc8ad76cbf2c3bfb30766762
  image:
    tag: v0.8.63
  ingress:
    hosts:
    - host: sample-spring.demo.spic.me
  istio:
    gateway:
      enabled: true
    sidecar:
      inject: true
  serviceMonitor:
    enabled: true
    endpoints:
    - interval: 10s
      path: /actuator/prometheus
      port: http
    selector:
      release: prometheus-eks-demo
