app:
  configmap:
    data:
      CLUSTER_NAME: eks-demo
      HOSTNAME: sample.svc.cluster.local
      MESSAGE: hello world
      PROFILE: alpha
      PROTOCOL: http
      VERSION: v0.8.62
    enabled: true
  env:
  - name: ENV_HASH
    value: c58e671d16b52c82fe095dfc3c3e1db5
  image:
    tag: v0.8.62
  ingress:
    hosts:
    - host: sample-spring.demo.spic.me
  istio:
    gateway:
      enabled: true
    sidecar:
      inject: true
  serviceMonitor:
    enabled: true
    endpoints:
    - interval: 10s
      path: /actuator/prometheus
      port: http
    selector:
      release: prometheus-eks-demo
