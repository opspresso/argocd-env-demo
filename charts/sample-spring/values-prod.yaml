app:
  configmap:
    data:
      CLUSTER_NAME: eks-demo
      HOSTNAME: sample.svc.cluster.local
      MESSAGE: hello world
      PROFILE: prod
      PROTOCOL: http
      VERSION: v0.9.8
    enabled: true
  env:
  - name: ENV_HASH
    value: eeba62c66615b85f351c9f9196fbce37
  image:
    tag: v0.9.8
  ingress:
    hosts:
    - host: sample-spring.demo.nalbam.com
    preview:
      enabled: true
      hosts:
      - host: sample-spring-preview.demo.nalbam.com
  istio:
    gateway:
      enabled: true
    sidecar:
      inject: true
  serviceMonitor:
    enabled: true
    endpoints:
    - interval: 10s
      path: /actuator/prometheus
      port: http
    selector:
      release: prometheus-eks-demo
