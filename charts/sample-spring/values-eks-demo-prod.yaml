app:
  configmap:
    data:
      CLUSTER_NAME: eks-demo
      HOSTNAME: sample.svc.cluster.local
      MESSAGE: hello world
      PROFILE: prod
      PROTOCOL: http
      VERSION: v0.8.60
    enabled: true
  env:
  - name: ENV_HASH
    value: 7100d0faa1f556aaa9fa4601434fddda
  image:
    tag: v0.8.60
  ingress:
    hosts:
    - host: sample-spring.demo.spic.me
  istio:
    gateway:
      enabled: true
    sidecar:
      inject: true
  serviceMonitor:
    enabled: true
    endpoints:
    - interval: 10s
      path: /actuator/prometheus
      port: http
    selector:
      release: prometheus-eks-demo
