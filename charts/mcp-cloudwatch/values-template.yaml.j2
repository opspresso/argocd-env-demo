app:
  replicaCount: {{replicas}}
{% if env != 'k3s' and not resources %}
  resources: null
{% endif %}
{% if env != 'k3s' %}
  podLabels:
    sidecar.istio.io/inject: "true"
{% endif %}

  configmap:
    enabled: true
    data:
      AWS_REGION: {{ aws_region }}
